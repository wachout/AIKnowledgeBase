# AIKnowledgeBase - 智能知识库与多智能体协作系统

<p align="center">
  <img src="https://img.shields.io/badge/Python-3.9+-blue.svg" alt="Python 3.9+">
  <img src="https://img.shields.io/badge/License-Apache%202.0-green.svg" alt="License: Apache 2.0">
  <img src="https://img.shields.io/badge/LLM-Multi--Model-orange.svg" alt="Multi-Model LLM">
  <img src="https://img.shields.io/badge/Architecture-Multi--Agent-purple.svg" alt="Multi-Agent">
</p>

<p align="center">
  <b>基于大语言模型的智能知识库系统，支持四层多智能体协作决策、RAG检索增强生成、智能SQL生成和可视化分析</b>
</p>

---

## 📖 项目简介

AIKnowledgeBase 是一个企业级智能知识库解决方案，集成了多种先进的 AI 技术，包括：

- 🤖 **四层多智能体协作系统**：决策层 → 实施层 → 具体实施模拟层 → 检测层
- 🔍 **RAG 检索增强生成**：支持向量检索、图数据库检索、混合检索
- 💬 **智能 SQL 生成**：自然语言转 SQL，支持多轮对话优化
- 📊 **智能可视化**：自动生成 ECharts 图表和数据报表
- 🗄️ **多数据源支持**：Milvus、Neo4j、Elasticsearch、MySQL、PostgreSQL

---

## 🏗️ 系统架构

```
┌─────────────────────────────────────────────────────────────────┐
│                        前端层 (React + Vite)                     │
│                     提供用户交互界面                             │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│                      API 服务层 (Flask)                          │
│              处理 HTTP 请求，管理会话状态                        │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│                      业务控制层 (Control)                        │
│    ┌──────────┬──────────┬──────────┬──────────┬──────────┐    │
│    │ Chat控制 │ 讨论控制 │ SQL控制  │ 文件控制 │ 搜索控制 │    │
│    └──────────┴──────────┴──────────┴──────────┴──────────┘    │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│                      智能体层 (Agent)                            │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐   │
│  │ AgenticRAG │ │AgenticQuery│ │AgenticSQL  │ │ TableFile  │   │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘   │
│  ┌────────────┐ ┌────────────┐ ┌────────────┐ ┌────────────┐   │
│  │  Echarts   │ │   Theme    │ │  File分析  │ │  EmbGraph  │   │
│  └────────────┘ └────────────┘ └────────────┘ └────────────┘   │
└─────────────────────────────┬───────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│                  四层多智能体协作系统 (Roles)                    │
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 第一层：圆桌讨论系统 (Roundtable Discussion)             │   │
│  │  - Scholar(学者) → Moderator(主持人) → DomainExperts    │   │
│  │  - Skeptic(质疑者) → Synthesizer(综合者)                │   │
│  │  - 输出：决策方案、任务分解、风险评估                    │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 第二层：实施讨论组 (Implementation Layer)                │   │
│  │  - 动态创建实施专家                                     │   │
│  │  - 四阶段讨论：设计→评审→风险→共识                      │   │
│  │  - 输出：详细实施计划、执行步骤                          │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 第三层：具体实施模拟层 (Execution Simulation)            │   │
│  │  - 模拟执行专家 (硬件/软件/视觉/算法/测试)               │   │
│  │  - 输出具体指标：材质/尺寸/配置/代码规格                 │   │
│  │  - 生成「任务实施规格书」                                │   │
│  └─────────────────────────────────────────────────────────┘   │
│                              ↓                                  │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │ 第四层：检测讨论组 (Validation Layer)                    │   │
│  │  - 检验第三层输出                                       │   │
│  │  - 验证指标合理性                                       │   │
│  └─────────────────────────────────────────────────────────┘   │
└─────────────────────────────────────────────────────────────────┘
                              │
┌─────────────────────────────▼───────────────────────────────────┐
│                      数据层 (Database)                           │
│  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐          │
│  │  Milvus  │ │  Neo4j   │ │  Elastic │ │  SQLite  │          │
│  │ (向量库) │ │  (图库)  │ │  (搜索)  │ │  (会话)  │          │
│  └──────────┘ └──────────┘ └──────────┘ └──────────┘          │
│  ┌──────────┐ ┌──────────┐                                      │
│  │  MySQL   │ │ PostgreSQL│                                     │
│  │ (业务库) │ │ (业务库) │                                      │
│  └──────────┘ └──────────┘                                      │
└─────────────────────────────────────────────────────────────────┘
```

---

## ✨ 核心功能

### 1. 🤖 四层多智能体协作系统

| 层级 | 名称 | 功能 | 输出 |
|------|------|------|------|
| 第一层 | 圆桌讨论系统 | 任务分析、角色分配、深度讨论、共识追踪 | 决策方案、任务分解 |
| 第二层 | 实施讨论组 | 详细实施计划、步骤设计、风险评估 | 实施计划、执行步骤 |
| 第三层 | 具体实施模拟层 | 模拟执行、输出具体指标、生成规格书 | 任务实施规格书 |
| 第四层 | 检测讨论组 | 检验输出、验证合理性 | 验证报告 |

### 2. 🔍 RAG 检索增强生成

- **混合检索**：向量检索 + 关键词检索 + 图遍历
- **意图识别**：自动识别用户查询意图
- **多轮对话**：支持上下文感知的连续对话
- **结果评估**：自动评估检索结果质量

### 3. 💬 智能 SQL 生成

- **自然语言转 SQL**：支持复杂查询、多表关联
- **数据库分析**：自动分析表结构、字段含义
- **SQL 优化**：自动优化查询性能
- **结果解释**：将 SQL 结果转换为自然语言

### 4. 📊 智能可视化

- **ECharts 图表**：自动生成柱状图、折线图、饼图等
- **数据报表**：自动生成结构化数据报表
- **文件分析**：支持 Excel、CSV 等文件分析
- **统计分析**：自动计算统计指标

---

## 🚀 快速开始

### 环境要求

- Python 3.9+
- Node.js 16+ (前端)
- 支持的向量数据库：Milvus / Chroma
- 支持的图数据库：Neo4j

### 安装步骤

#### 1. 克隆项目

```bash
git clone https://github.com/your-username/AIKnowledgeBase.git
cd AIKnowledgeBase
```

#### 2. 安装后端依赖

```bash
cd src
pip install -r ../requirements.txt
```

#### 3. 配置环境变量

```bash
cp .env.example .env
# 编辑 .env 文件，配置以下关键参数：
# - LLM API Key (支持 OpenAI、通义千问、DeepSeek 等)
# - 数据库连接信息
# - 向量数据库配置
```

#### 4. 安装前端依赖

```bash
cd ../app
npm install
```

#### 5. 启动服务

```bash
# 启动后端服务
cd ../src
python run_knowledge_base.py

# 启动前端服务 (新终端)
cd ../app
npm run dev
```

#### 6. 访问系统

打开浏览器访问：`http://localhost:5173`

---

## 📁 项目结构

```
AIKnowledgeBase/
├── app/                          # 前端应用 (React + TypeScript + Vite)
│   ├── src/
│   │   ├── components/           # UI 组件
│   │   ├── services/             # API 服务
│   │   ├── types/                # TypeScript 类型
│   │   └── App.tsx               # 主应用
│   └── package.json
│
├── src/                          # 后端核心代码
│   ├── Agent/                    # 智能体代理
│   │   ├── AgenticRagAgent/      # RAG 智能体
│   │   ├── AgenticQueryAgent/    # 查询智能体
│   │   ├── AgenticSqlAgent/      # SQL 智能体
│   │   ├── TableFileAgent/       # 文件分析智能体
│   │   └── ...
│   │
│   ├── Roles/                    # 四层多智能体协作系统
│   │   ├── hierarchy/            # 层次化智能体系统
│   │   │   ├── layers/           # 四层实现
│   │   │   │   ├── decision_layer.py
│   │   │   │   ├── implementation_layer.py
│   │   │   │   └── validation_layer.py
│   │   │   ├── agents/           # 智能体实现
│   │   │   └── types.py          # 类型定义
│   │   ├── personnel/            # 圆桌讨论角色
│   │   │   ├── base_agent.py     # 智能体基类
│   │   │   ├── scholar.py        # 学者
│   │   │   ├── moderator.py      # 主持人
│   │   │   ├── domain_expert.py  # 领域专家
│   │   │   └── ...
│   │   ├── roundtable/           # 圆桌讨论核心
│   │   └── tools/                # 工具系统
│   │
│   ├── Control/                  # 业务控制层
│   ├── Config/                   # 配置管理
│   ├── Db/                       # 数据库连接层
│   ├── Emb/                      # 嵌入模型
│   ├── Graphrag/                 # 图 RAG
│   └── Servers/                  # 服务层
│
├── requirements.txt              # Python 依赖
└── README.md                     # 项目说明
```

---

## 🔧 配置说明

### LLM 配置

支持多种大语言模型，在 `src/conf/config.ini` 中配置：

```ini
[base_url]
# 通义千问配置
agent_code_name=qwen3-30b-a3b
agent_base_url=https://dashscope.aliyuncs.com/compatible-mode/v1
agent_key=your-api-key

# 或 DeepSeek 配置
# agent_code_name=deepseek-reasoner
# agent_base_url=https://api.deepseek.com/v1
# agent_key=your-api-key
```

### 数据库配置

#### Neo4j (图数据库)
```ini
[neo4j_uri]
neo4j_uri=bolt://localhost:7687
neo4j_user=neo4j
neo4j_password=your-password
```

#### Milvus (向量数据库)
```ini
[milvus]
host=localhost
port=19530
collection_name=your_collection
```

---

## 🎯 使用示例

### 1. 启动圆桌讨论

```python
from Roles import RoundtableDiscussion
from Config.llm_config import get_chat_tongyi

# 初始化讨论系统
llm = get_chat_tongyi()
discussion = RoundtableDiscussion(llm_instance=llm)

# 运行讨论
topic = "如何设计一个高性能的推荐系统？"
async for chunk in discussion.run_discussion(topic):
    print(chunk, end="")
```

### 2. 使用 RAG 查询

```python
from Agent.agentic_rag_run import run_rag_query

# 执行 RAG 查询
result = run_rag_query(
    query="公司的年假政策是什么？",
    knowledge_base_id="hr_policies"
)
print(result["answer"])
```

### 3. 自然语言转 SQL

```python
from Agent.agentic_sql_run import run_sql_agent

# 生成 SQL
result = run_sql_agent(
    query="查询去年销售额前10的客户",
    database_id="sales_db"
)
print(result["sql"])
print(result["result"])
```

---

## 🤝 贡献指南

欢迎提交 Issue 和 Pull Request！

### 开发流程

1. Fork 项目
2. 创建功能分支 (`git checkout -b feature/amazing-feature`)
3. 提交更改 (`git commit -m 'Add amazing feature'`)
4. 推送分支 (`git push origin feature/amazing-feature`)
5. 创建 Pull Request

### 代码规范

- 遵循 PEP 8 Python 代码规范
- 使用类型注解
- 编写单元测试
- 更新文档

---

## 📄 许可证

本项目采用 [Apache 2.0](LICENSE) 许可证开源。

---

## 🙏 致谢

感谢以下开源项目的支持：

- [LangChain](https://github.com/langchain-ai/langchain) - LLM 应用框架
- [LangGraph](https://github.com/langchain-ai/langgraph) - 工作流编排
- [Milvus](https://github.com/milvus-io/milvus) - 向量数据库
- [Neo4j](https://github.com/neo4j/neo4j) - 图数据库

---

## 📞 联系我们

- 项目主页：[https://github.com/your-username/AIKnowledgeBase](https://github.com/your-username/AIKnowledgeBase)
- 问题反馈：[Issues](https://github.com/your-username/AIKnowledgeBase/issues)
- 邮件联系：your-email@example.com

---

<p align="center">
  <b>如果这个项目对您有帮助，请给我们一个 ⭐ Star！</b>
</p>
